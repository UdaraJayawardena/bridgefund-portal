import { createStyles } from "@material-ui/core";
import { lightBlue } from "assets/jss/material-dashboard-react";

const baiOverviewstyle = () => createStyles({
  blueIconButton: {
    color: "#fff",
    backgroundColor: lightBlue,
    borderRadius: 20,
  },
  zeroMargin: {
    margin: 0
  },
});

export default baiOverviewstyle;